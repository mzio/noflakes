import React from "react";

export default class CreatePactUsers extends React.Component {
  constructor(props) {
    super(props);
    // On mounting, query the backend for the associated users?
    // Before this, have to search for users,
  }
}
